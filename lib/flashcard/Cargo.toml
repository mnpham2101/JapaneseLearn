[package]
name = "flashcard"
version = "0.1.0"
edition = "2021"
links = "flashcard"

[dependencies]
slint = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
slint-build = { workspace = true }
