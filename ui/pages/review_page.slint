import { CommonList } from "../components/common_list.slint";
import { CommonButton } from "../components/common_button.slint";
import { Flashcard, FlashcardModel } from "../components/flashcard.slint";

export component ReviewPage {
    width: 100%;
    height: 100%;

    Rectangle {
        width: 100%;
        height: 100%;
        background: #ffffff;

        VerticalLayout {
            padding: 20px;
            spacing: 20px;

            HorizontalLayout {
                spacing: 10px;
                alignment: center;

                Text {
                    text: "Review Mode";
                    font-size: 28px;
                    color: #333333;
                }

                CommonButton {
                    width: 140px;
                    text: "Review All";
                    primary: true;
                    clicked => {
                        // Placeholder action for review all
                    }
                }
            }

            Flashcard {
                width: 360px;
                data: {
                    jap-obj: "こんにちは",
                    explanation: "Hello / Good afternoon",
                };
            }
        }
    }
}
