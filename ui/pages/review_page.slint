import { CommonList } from "../components/common_list.slint";
import { CommonButton } from "../components/common_button.slint";

export component ReviewPage {
    width: 100%;
    height: 100%;

    Rectangle {
        width: 100%;
        height: 100%;
        background: #ffffff;

        VerticalLayout {
            padding: 20px;
            spacing: 20px;

            HorizontalLayout {
                spacing: 10px;
                alignment: center;

                Text {
                    text: "Review Mode";
                    font-size: 28px;
                    color: #333333;
                }

                CommonButton {
                    width: 140px;
                    text: "Review All";
                    primary: true;
                    clicked => {
                        // Placeholder action for review all
                    }
                }
            }

            CommonList {
                title: "Flashcards";
                items: ["こんにちは - Hello", "ありがとう - Thank you", "さようなら - Goodbye"];
                addButtonText: "Add Flashcard";
                addClicked => {
                    // Placeholder action for add flashcard
                }
            }
        }
    }
}
