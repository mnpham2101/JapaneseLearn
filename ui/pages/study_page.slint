import { CommonList } from "../components/common_list.slint";
import { CommonButton } from "../components/common_button.slint";

export component StudyPage {
    width: 100%;
    height: 100%;

    Rectangle {
        width: 100%;
        height: 100%;
        background: #ffffff;

        VerticalLayout {
            padding: 20px;
            spacing: 20px;

            HorizontalLayout {
                spacing: 10px;
                alignment: center;

                Text {
                    text: "Study Mode";
                    font-size: 28px;
                    color: #333333;
                }

                CommonButton {
                    width: 140px;
                    text: "Create Stack";
                    primary: true;
                    clicked => {
                        // Placeholder action for create stack
                    }
                }
            }

            CommonList {
                title: "Flashcard Stacks";
                items: ["Hiragana Basics", "N5 Vocabulary", "Travel Phrases"];
                addButtonText: "Add Stack";
                addClicked => {
                    // Placeholder action for add stack
                }
            }
        }
    }
}
