import { Tokens } from "@styles";

export component VocabularyPage {
    width: 100%;
    height: 100%;

    Rectangle {
        width: 100%;
        height: 100%;
        background: Tokens.page-background;

        Text {
            text: "Vocabulary";
            font-size: Tokens.font-size-heading;
            color: Tokens.text-primary;
        }
    }
}
