[package]
name = "japanese_learn"
version = "0.1.0"
authors = [""]
edition = "2021"
description = "Japanese learning flashcards app using Rust and Slint."
license = "MIT OR Apache-2.0"
readme = "README.md"
build = "build.rs"

[dependencies]
slint = "0.3.5"

[build-dependencies]
slint-build = "0.3.5"
